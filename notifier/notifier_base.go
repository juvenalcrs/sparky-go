package notifier

/*
	type MyNotifier struct {
		notifier.Base
	}


	cancel := myNotifier.Watch(func() {
		// update widgets
	})
	cancel() => call on Destroy

	cancel := myNotifier.WatchOnly(func (n *MyNotifier) bool {
		return n.PrevState().Field1 > n.State().Field1
	}, func() {
		// update widgets
	})
	cancel() => call on Destroy

*/
